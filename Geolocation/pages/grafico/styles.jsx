import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F8F8FF', // fundo claro
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 20,
    color: '#8B8DFE', // roxo claro
  },
  chart: {
    marginTop: 20,
    borderRadius: 16,
    backgroundColor: '#FFFFFF', // fundo branco do gráfico
  },
});
